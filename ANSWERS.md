## Questions

1. What is the difference between new and create for a model?
  New just instantiates the instance, whereas create both instantiates it and saves it to the database.

2. What command combined with new will emulate the same behavior as create?
  Save

3. What is the default integer column that exists on every table but we did NOT define?
  id

4. What single line of ruby code can insert a cat with the name "Kira" in the database?
  Cat.create(:name => "Kira")

5. How did you like this homework in comparison with the previous homeworks?
  I enjoyed it more since we're getting more involved and doing more complex and interconnected things instead of simple individual tasks.
